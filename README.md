This project is a Player Performance & Salary Value Analyzer built using Python with a graphical user interface (GUI).

It allows users to input player statistics and salary, then calculates a performance score and value score to determine if a player is undervalued, fairly paid, or overpaid.

The application also provides recommendations, tracks multiple players, and displays results in a table with summary insights.

Language: Python  
Libraries: tkinter
